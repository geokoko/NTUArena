ignore-scripts=true
# Exception: backend's mongodb-memory-server uses a postinstall hook to prefetch mongod binaries for tests.
# Leave scripts disabled by default; only allow scripts for that install path if a fresh test binary cache is required.
