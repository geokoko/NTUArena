# ============================================================
# NTUArena Environment Variables
# ============================================================
#   ALL VALUES BELOW ARE DUMMY / DEVELOPMENT DEFAULTS.
#   Replace them with real values before deploying to production.
# ============================================================

# --- Backend ---
PORT=5000
NODE_ENV=development
AUTH_ENABLED=true

# --- JWT ---
#   DUMMY VALUE — generate a strong random secret for production
#   e.g.: openssl rand -base64 64
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d

# --- MongoDB ---
#   DUMMY CREDENTIALS — set real credentials in production
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=change-me-in-production
MONGO_DB=ntuarena
MONGO_URI=mongodb://admin:change-me-in-production@mongo:27017/ntuarena?replicaSet=rs0&authSource=admin

# --- Redis ---
#   DUMMY PASSWORD — set a real password in production
REDIS_PASSWORD=change-me-in-production
REDIS_URL=redis://:change-me-in-production@redis:6379
